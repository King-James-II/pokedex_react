import React from "react"; // Import React library
import ReactDOM from "react-dom"; // Import ReactDOM library
import App from "./App.jsx"; // Import main App component

// Render the App component into the root element of the HTML document
ReactDOM.render(
  <App />, // JSX element representing the App component
  document.getElementById('root') // Root element in the HTML document
);
